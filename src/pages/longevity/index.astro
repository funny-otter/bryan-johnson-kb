---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ProtocolSections from '../../components/ProtocolSections.astro';
import { protocolCategories } from '../../data/protocols.mjs';
const protocol = protocolCategories.find((item) => item.category === 'longevity')!;
---
<BaseLayout title="Longevity protocol · BJ.WATCH" description="Source-aware Bryan Johnson longevity protocol notes with claim framing." section="longevity">
  <section class="protocol-page" aria-labelledby="protocol-title">
    <p class="eyebrow">// PROTOCOLS / longevity</p>
    <h1 id="protocol-title">{protocol.title}</h1>
    <p class="lede">{protocol.lede}</p>
    <p class="source-aware-note">Source-aware summary only; not medical advice. Claims about biological age, enhancement, or immortality remain claims unless independently validated.</p>
    <ProtocolSections sections={protocol.sections} title={`${protocol.title}: habits, longterm, and don’ts`} />
    <div class="protocol-grid">
      {protocol.cards.map((card) => (
        <article class="protocol-card">
          <span>{card.label}</span>
          <h2>{card.title}</h2>
          <p>{card.body}</p>
        </article>
      ))}
    </div>
    <section class="source-panel" aria-labelledby="longevity-sources">
      <h2 id="longevity-sources">Source trails</h2>
      <div class="source-chips">{protocol.sourcePaths.map((path) => <code>{path}</code>)}</div>
    </section>
  </section>
</BaseLayout>
